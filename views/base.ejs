<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title><%= config.title %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style type="text/css">
    body {
      margin: 20px;
    }
    a:link, a:visited {
      text-decoration: none;
      color: rgb(96, 143, 219);
    }
    a:hover, a:active {
      text-decoration: underline;
    }
    .id {
      font-family: monospace;
    }
    </style>
  </head>
  <body>
    <h1><%= config.title %></h1>
    <p>
      <%= config.description %>
    </p>
    <p>
      See documentation <a href="https://github.com/gbv/validation-service" target="_blank">on GitHub</a> for details.
    </p>
    <h3>API Endpoints</h3>
    <ul>
      <li>GET/POST <a href="validate">/validate</a> - validate data (<a href="https://github.com/gbv/validation-service#get-validate" target="_blank">documentation</a>)</li>
      <li>GET <a href="formats">/formats</a> - list supported data formats (<a href="https://github.com/gbv/validation-service#get-formats" target="_blank">documentation</a>)</li>
      <li>GET <a href="schema">/schema</a> - get a schema file (<a href="https://github.com/gbv/validation-service#get-schema" target="_blank">documentation</a>)</li>
      <li>GET <a href="types">/types</a> - list supported schema types (<a href="https://github.com/gbv/validation-service#get-types" target="_blank">documentation</a>)</li>
    </ul>
    <h3>Formats</h3>
    <p>The following data formats can be validated:</p>
    <ul>
      <% for (var format of config.formats) { %>
        <li>
          <a href="formats?format=<%= format.id %>" class="id"><%= format.id %></a>
          <% if (format.title) { %> (<%= format.title %>)<% } %>
        </li>
      <% } %>
    </ul>
    <h3>Schema types</h3>
    <p>Validation can be perfomed based on schemas in the following schema languages:</p>
    <ul>
      <% for (var type of config.types) { %>
        <li>
          <a href="types?type=<%= type.id %>" class="id"><%= type.id %></a>
          <% if (type.title) { %> (<%= type.title %>)<% } %>
        </li>
      <% } %>
    </ul>
  </body>
  <footer>
    <p>
      validation-service version <%= config.version %>
    </p>
  </footer>
</html>
